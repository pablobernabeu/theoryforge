# Opt-in embedding-based construct-redundancy screen.

Results depend on a user-supplied embedding function whose outputs are
not deterministic across model versions or SDKs. This screen is the
assistive counterpart to the deterministic lexical screen in
`tf_redundancy_check`.
