# Assemble a reviewer-facing audit bundle as a single Markdown document.

Composes the deterministic rigour report, severity table, provenance,
and preregistration document into one byte-identical bundle. See
API_SPEC.md section 19.
