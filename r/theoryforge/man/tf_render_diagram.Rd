% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{tf_render_diagram}
\alias{tf_render_diagram}
\title{Render a diagram in the viewer or as SVG}
\usage{
tf_render_diagram(x, type = "nomological_net", as = c("widget", "svg"))
}
\arguments{
\item{x}{A theory object (named list), or a diagram IR string from
\code{\link[=tf_diagram]{tf_diagram()}} or \code{\link[=tf_lit_diagram]{tf_lit_diagram()}}, so literature diagrams render the
same way.}

\item{type}{The diagram type, as in \code{\link[=tf_diagram]{tf_diagram()}}. Ignored when \code{x} is
already an IR string.}

\item{as}{Either \code{"widget"} (default), an htmlwidget for the viewer and for
R Markdown, or \code{"svg"}, a standalone SVG string.}
}
\value{
An htmlwidget when \code{as = "widget"}; a single SVG string when
\code{as = "svg"}.
}
\description{
Renders a digraph view of a theory without leaving R. Where \code{\link[=tf_diagram]{tf_diagram()}}
returns the deterministic Graphviz DOT string, \code{tf_render_diagram()} passes
that string to the DiagrammeR engine and returns either an interactive
widget, which displays in the RStudio viewer and in R Markdown documents, or
a standalone SVG string, ready to embed in a page or save to a file.
}
\details{
The three chart views (\code{venn}, \code{rigour} and \code{severity}) are already SVG, so
they are returned as-is under \code{as = "svg"} and wrapped for display under
\code{as = "widget"}. The \code{causal_dag} view emits dagitty syntax rather than DOT,
so it is not rendered here; paste it into a dagitty tool or the dagitty R
package instead.
}
\examples{
\dontshow{if (requireNamespace("DiagrammeR", quietly = TRUE)) withAutoprint(\{ # examplesIf}
theory <- tf_theory("demo-1", "A demonstration theory") |>
  tf_add_construct("c_arousal", "Arousal", "Bodily activation.") |>
  tf_add_construct("c_threat", "Perceived threat", "Appraised danger.") |>
  tf_add_proposition("p1", "c_arousal", "c_threat", "causes")
tf_render_diagram(theory, "nomological_net")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tf_diagram]{tf_diagram()}} for the intermediate representation itself, which
needs no optional packages and stays byte-identical across the R and
Python implementations.
}
