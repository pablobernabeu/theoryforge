# Force LF for all text files so the byte-identical golden artifacts (and the
# cross-language parity contract) hold identically on every platform.
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.pdf binary
*.docx binary
*.gz binary
*.zip binary
