# Build artefacts: the vendored package source is regenerated by build.mjs
# (locally before serving, and in CI before deploying), so it never needs to be
# committed and can never go stale.
r/vendor/
py/vendor/
